version: "2"
services:
    pvit-web:
        restart: always
        build: .
        ports: 
            - "1127:3000"
        links:
            - pvitredis
    pvitredis:
        restart: always
        image: redis
